package com.schwab.assessment.orchestration.model;

/**
 * Confidence level attached to an assumption the engine makes when
 * resolving a requirements ambiguity without human input.
 */
public enum Confidence {
    HIGH,
    MEDIUM,
    LOW
}
